# Normalize text to LF in the repo — avoids CRLF churn between Windows and CI.
* text=auto eol=lf

# Treat large simulation artifacts as binary (defensive; they're gitignored too).
*.bin binary
*.dat binary
*.npy binary
